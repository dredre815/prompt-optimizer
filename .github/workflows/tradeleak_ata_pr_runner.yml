name: TradeLeak ATA PR Runner

on:
  pull_request:
    branches: [main]
    paths:
      - 'tradeleak_ata_trigger/**'

permissions:
  contents: read
  issues: write
  actions: read

jobs:
  pilot:
    runs-on: ubuntu-latest
    timeout-minutes: 90
    env:
      TRADELEAK_ISSUE_NUMBER: '27'
      ATA_COMMIT: '6b49ee837ebc1fa0d5bf99d655dc3adc352a77d3'
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - name: Run frozen credential-isolated pilot
        shell: bash
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          set -euo pipefail
          chmod +x tradeleak_ata/bootstrap.sh
          bash tradeleak_ata/bootstrap.sh
      - name: Upload non-secret results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: tradeleak-ata-results-${{ github.run_id }}
          path: tradeleak_ata_output
          if-no-files-found: warn
          retention-days: 14
